CREATE SCHEMA Campo;
USE campo;


CREATE TABLE Condicion_Fisca(
id_condicion_fiscal CHAR(2) NOT NULL PRIMARY KEY,
Condicion_fiscal VARCHAR (50)
);

CREATE TABLE Clase_de_animal(
id_clase CHAR(2) NOT NULL PRIMARY KEY,
CLASE VARCHAR (50)
);

CREATE TABLE Categoria_de_animal(
id_categoria CHAR(2) NOT NULL PRIMARY KEY,
Categoria VARCHAR (50)
);


CREATE TABLE Suplementos(
ID_suplementos CHAR(2) NOT NULL PRIMARY KEY,
SUPLEMENTO VARCHAR (50)
);


CREATE TABLE PROVEEDORES(
ID_PROVEEDOR CHAR(2)  NOT NULL PRIMARY KEY,
CUIT VARCHAR (13) NOT NULL, 
NOMBRE VARCHAR (20),
APELLIDO VARCHAR (20) NOT NULL,
DIRECCION VARCHAR (25), 
TELEFONO VARCHAR (30),
EMAIL VARCHAR (30),
ID_CONDICION_FISCAL CHAR (2)  NOT NULL, -- foreign key
foreign key(ID_CONDICION_FISCAL) REFERENCES Condicion_Fisca (id_condicion_fiscal)
);	


CREATE TABLE PADRES (
ID_PADRES CHAR(2) NOT NULL PRIMARY KEY,
NUMERO_CARABANA VARCHAR (10) NOT NULL,
CLASE CHAR(2) , -- foreign key
CATEGORIA CHAR(2) NOT NULL, -- foreign key
ID_PROVEEDOR CHAR(2) NOT NULL, -- foreign key
DESCRIPCION VARCHAR (50),
FEHCA_INGRESO DATE NOT NULL,
COSTO decimal NOT NULL,
FECHA_EGRESO DATE,
PRECIO_VENTA DECIMAL ,
MOTIVO_EGRESO VARCHAR (50),
FOREIGN KEY (CLASE) REFERENCES Clase_de_animal (id_clase),
FOREIGN KEY (CATEGORIA) REFERENCES Categoria_de_animal (id_categoria),
foreign key(ID_PROVEEDOR) REFERENCES PROVEEDORES (ID_PROVEEDOR)
);


CREATE TABLE MADRES (
ID_MADRES CHAR(4) NOT NULL PRIMARY KEY,
NUMERO_CARABANA VARCHAR (10) NOT NULL,
CLASE CHAR(2), -- FOREIGN KEY
CATEGORIA CHAR(2) NOT NULL, -- FOREIGN KEY
ID_PROVEEDOR CHAR(2) NOT NULL, -- FOREIGN KEY
COMENTARIO VARCHAR (50),
FEHCA_INGRESO DATE NOT NULL,
COSTO DECIMAL NOT NULL,
FECHA_EGRESO DATE,
PRECIO_VENTA DECIMAL,
MOTIVO_EGRESO VARCHAR (50),
FOREIGN KEY (CLASE) REFERENCES Clase_de_animal (id_clase),
FOREIGN KEY (CATEGORIA) REFERENCES Categoria_de_animal (id_categoria),
foreign key(ID_PROVEEDOR) REFERENCES PROVEEDORES (ID_PROVEEDOR)

);


CREATE TABLE CLIENTES (
ID_CLIENTES CHAR(2) NOT NULL PRIMARY KEY,
CUIT VARCHAR(13) NOT NULL, 
NOMBRE VARCHAR(20),
APELLIDO VARCHAR(20) NOT NULL,
DIRECCION VARCHAR(25),
TELEFONO INT,
EMAIL VARCHAR(30),
ID_CONDICION_FISCAL CHAR(2) NOT NULL,
FOREIGN KEY (ID_CONDICION_FISCAL) REFERENCES Condicion_Fisca (id_condicion_fiscal)
);


CREATE TABLE CRIAS (
ID_CRIAS CHAR(5)  NOT NULL PRIMARY KEY,
NUMERO_CARABANA VARCHAR (10) NOT NULL,
CLASE CHAR(2) NOT NULL, -- FOREIGN KEY
CATEGORIA CHAR(2) NOT NULL, -- FOREIGN KEY
ID_PROVEEDOR CHAR(2) NOT NULL, -- FOREIGN KEY
ID_MADRE CHAR(4) NOT NULL, -- FOREIGN KEY
COMENTARIO VARCHAR (90), 
FECHA_INGRESO DATE NOT NULL,
COSTO DECIMAL,
FECHA_DESTETE DATE,
PESO_DESTETE DECIMAL,
FECHA_ENTRADA_ENGORDE DATE,
PESO_ENTRADA_ENGORDE DECIMAL,
SUPLEMENTOS CHAR(2) NOT NULL, -- FOREIGN KEY
MOTIVO_EGRESO VARCHAR (90),
FOREIGN KEY (CLASE) REFERENCES Clase_de_animal (id_clase),
FOREIGN KEY (CATEGORIA) REFERENCES Categoria_de_animal (id_categoria),
foreign key (ID_PROVEEDOR) REFERENCES PROVEEDORES (ID_PROVEEDOR),
FOREIGN KEY (ID_MADRE) REFERENCES MADRES (ID_MADRES),
FOREIGN KEY (SUPLEMENTOS) REFERENCES SUPLEMENTOS (ID_suplementos)

);




